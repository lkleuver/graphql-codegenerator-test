query CheckProduct($product: ProductCheckInput!) {
    check(product: $product) {
        success
        product {
            services {
                ...AServiceFragment
                ...on AService {
                    dooms {
                        code
                        loads {
                            code
                        }
                    }
                }
            }
            notes
            rules
        }
    }
}